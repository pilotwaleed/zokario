# macOS
.DS_Store
.AppleDouble
.LSOverride

# Environment
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*

# Local server
.claude/serve.mjs.log

# Editor config (optional, remove if you want to commit)
.vscode/
.idea/

# Node (if using npm later)
node_modules/
package-lock.json

# Stripe test keys (keep placeholders only)
# .env with real keys should never be committed

# Internal reports & dev tooling — repo is PUBLIC, keep these local only
# (also excluded from the FTP deploy in .github/workflows/deploy.yml)
docs/
tools/
